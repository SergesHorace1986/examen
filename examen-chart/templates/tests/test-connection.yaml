apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "examen-chart.fullname" . }}-test-connection"
  labels:
    {{- include "examen-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "examen-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
