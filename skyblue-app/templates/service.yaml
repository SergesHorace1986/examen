apiVersion: v1
kind: Service
metadata:
  name: {{ include "skyblue-app.fullname" . }}
  labels:
    {{- include "skyblue-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "skyblue-app.selectorLabels" . | nindent 4 }}
